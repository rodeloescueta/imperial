# Database (PostgreSQL)
# Default Docker setup uses port 5433 to avoid conflicts with local PostgreSQL
DATABASE_URL="postgresql://imperial_admin:imperial_dev_2024@localhost:5433/imperial"
POSTGRES_PASSWORD="imperial_dev_2024"

# NextAuth.js / Auth.js
# Generate a secure secret for production: openssl rand -base64 32
AUTH_SECRET="your-secret-key-at-least-32-characters-long"
AUTH_URL="http://localhost:3011"
